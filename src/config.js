const base = "https://money-manager-cft3.onrender.com"

export const login = base + "/users/login"
export const resendOTP = base + "/verify/resendOtp";

export const me = base + "/users/me"
export const logout = base + "/users/logout"
export const logoutall = base + "/users/logoutAll"

//form
export const register = base + "/users"

//otp
export const verifyOTP = base + "/verify/otp"

export const addExpense = base + "/expense"
export const avatar = base + "/users/me/avatar"